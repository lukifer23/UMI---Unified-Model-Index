baseline: balanced_geometric
scenarios:
  - {name: balanced_geometric, formula: weighted_geometric_v1, alpha: 0.50}
  - {name: capability_heavy, formula: weighted_geometric_v1, alpha: 0.70}
  - {name: harmonic, formula: harmonic_mean_v1}
