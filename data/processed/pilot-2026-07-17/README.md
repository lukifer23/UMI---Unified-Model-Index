# Reproducible pilot outputs

Generated from `data/raw`, `config`, and `data/sources/registry.yaml` using the
checked-in `umi` CLI. `partial_scores` is diagnostic only: every `rank` and
`headline_overall` is null. `references.json` and `references.csv` contain a stable
long-form export of the external-index and attempted-task-cost observations.
