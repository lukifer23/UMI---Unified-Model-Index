# Open-ledger hunt for a later Overall headline

This note records a publication decision and the next acquisition targets. It is not a
scored artifact, not a ranking, and not a license grant. Checksums below are copied from
already-frozen registry entries or from previously verified public citations. No new
payload was downloaded for this note.

## Decision

Given no paid reruns, no invented Economics, and the current coverage hierarchy, the
highest-value product is:

1. **Ship UMI Public v0.5 Governed** from the frozen Epoch all-five 8+ extracts plus the
   two extra complete high-effort systems. That score is `umi_public`, not v0.3
   `headline_overall`.
2. **Keep the Opus / Kimi / GLM Capability certificate** as the v0.3 legacy ranking.
3. **Hunt DeepSWE-class public ledgers next** for billed Economics and the omitted
   v0.4 domains (context reliability, language/instruction, latency). Do not add empty
   workload families first.
4. **Do not promote published calculated cost-per-task into billed Economics.**
   WeirdML cost is Access Economics (`source_reported`) only. Artificial Analysis,
   CursorBench, Epoch ARC, DeepSWE LiteLLM dollars, and lab `$ / million` tariffs remain
   diagnostic for v0.3 Economics.

Adding a τ³-Banking tool-use family is the right *later* methodology step, but only in
the same change that admits a ready resource ledger for that family. Opening the family
now would spend a formula bump to document a hole we already know exists.

## Why this beats the alternatives

| Option | What we would get | Why it is worse now |
|---|---|---|
| Capability certificate + ledger hunt | A real 3-model ranking today; a written reject/admit list for the next agent | — |
| Add τ³ / GDPval / BrowseComp families first | Coverage still 0.045 / 0.000; `FORMULA_VERSION` changes | Methodology churn with no new score |
| Promote AA / Cursor cost columns | A fake Economics number | Invents `provider_billing_record` |
| Lower 0.50 / 0.40 / 0.60 gates | A headline by redefinition | Forbidden; weights are not redefined to manufacture eligibility |
| Pay for MMLU-Pro | At most +0.10 coverage | Out of scope, still below 0.50 / 0.40 |

Configured available families still max out at **0.35** Efficiency and **0.35** Economics
(coding 0.25 + general interaction 0.10). Research, tool-use, browser, and long-horizon
have no approved family. Completing DeepSWE + Terminal-Bench + CursorBench + MMLU-Pro
cannot emit `headline_overall`.

## Already frozen: admit or reject

| Candidate | Frozen status | Redistribution | Efficiency | Economics | Next action |
|---|---|---|---|---|---|
| DeepSWE harness means | Reviewed facts; official ledger cited at SHA-256 `13d6f7563330110231b008ae4eb38e03de24af08acead840de296d1127144971` | Facts and citations only | **Admitted** input / output / steps | Reject | Re-read `trials.json` only for endpoint / tier identity that could promote cache and wall. Do not promote LiteLLM dollars. |
| DeepSWE endpoint means | Same facts file; diagnostic records | Facts and citations only | Reject until deployment identity | Reject; Fable cost is 432/436 | Same ledger re-read |
| AA Terminal-Bench tokens | Reviewed public-page facts `2026-08-15` | Facts and citations only | Reject: provider tokens, no attempt rows, `cacheable ≠ cached` | No cost field | Hunt Harbor / Stanford raw dumps for the exact Max five |
| AA Omniscience tokens / calculated $ | Reviewed public-page facts | Facts and citations only | Reject: totals, not per-attempt means | Reject: calculated $ | Stay parked |
| AA τ³ / LCR / GDPval ops | Reviewed public-page facts; incomplete columns | Facts and citations only | Reject | Reject | Stay parked unless a per-task dump appears |
| CursorBench cost / tokens / steps | Reviewed public-page facts | Facts and citations only | Reject: not binary success; untyped tokens | Reject: table average | Stay parked |
| Epoch ARC cost-per-task | Inside frozen zip SHA-256 `35a7c21ba7d535514ebcf9bbe7b8265d2e2da40ef6b1fa63fe49323c3395a18b` | Full archive retained | Not a resource mean | Reject: metadata only | Stay parked |
| Lab token tariffs | Reviewed release facts | Facts and citations only | No | Reject: no task join | Stay parked |
| Fable AA / Cursor / Epoch rows | Present on source pages | Same as parent | Not usable | Not usable | Reject: Opus fallback or unverified routing |
| MMLU-Pro pack | Frozen MIT parquet SHA-256 `0e24a191921c2f453518a537a8b2117bd137e7714d4ef1565e9ba06c1ecb9ad8` | Full pack retained | Not scored | Not scored | Do not execute |

## External hunts (citation only; not frozen here)

These URLs were identified during planning. They are **not** admitted. Unknown
redistribution means facts-and-citations until a freeze records license, checksum,
and exact Max-effort identity.

1. **DeepSWE official ledger** — https://deepswe.datacurve.ai/artifacts/v1.1/trials.json  
   Already checksum-verified. Highest-priority re-read: does any trial row name
   serving endpoint, region, or service tier well enough to flip cache/wall from
   diagnostic to ready? Dollar fields stay diagnostic.

2. **Terminal-Bench / Harbor public result dumps** — Stanford / Laude Terminal-Bench
   leaderboard and Harbor `results.json` artifacts. Admit only if the dump covers
   the exact Max configurations (Opus 5, Fable 5, GPT-5.6 Sol, Kimi K3, GLM-5.2),
   a named harness (Terminus 2 / E2B or equivalent), binary success, and typed
   per-attempt or complete-cohort resource means.

3. **SWE-bench/experiments logs** — https://github.com/swe-bench/experiments  
   Public trajectories exist for other models. Most will fail exact named-release
   plus Max-effort plus fallback-absent identity for this pilot.

4. **Artificial Analysis databook / API** — https://artificialanalysis.ai/data-api/docs  
   Free tier exposes calculated cost-per-task. That is still a price-table replay.
   A Pro export is useful only if it includes per-task success **and** typed tokens
   **and** still cannot be treated as `provider_billing_record`.

5. **BrowseComp or other browser-agent public boards** — candidate for the empty
   `browser_computer_use` 0.15 category. Ingest only after license review and an
   exact Max crosswalk. Vendor-claimed BrowseComp scores are claims, not ready
   Efficiency.

## Admission bar (copy of the DeepSWE rule)

A new public source may enter ready Efficiency only when a frozen artifact has all of:

- exact named release, Max effort, fallback absent
- `attempts`, `successful_attempts`, and `success_rate` reconciling within `1e-12`
- per-metric observation count equal to `attempts`
- harness-level input / output / reasoning / turns / steps / tools (not one
  `tokens_per_task` blob)
- one interaction profile, operational-profile ID, success-definition ID, and cohort

Ready Economics additionally requires cost on every attempt, at least one success,
verified endpoint and service tier, and `billing_evidence = provider_billing_record`.

If the hunt finds no such ledger, the family stays empty and `headline_overall`
stays null. That is the correct outcome.

## What a reviewing agent should decide

- Confirm or reject Capability-first as the first official product.
- Confirm that empty-family expansion should wait for a ledger.
- Point at any **exact-Max** public dump this note missed, with URL, license, and
  whether it has attempt-level success plus typed resources.
- Do not recommend promoting calculated cost-per-task, lowering gates, or accepting
  Fable fallback rows.

The first official ranking artifact remains:

`data/pilots/v0.3/processed/comparison-certificate-three-model.json`
